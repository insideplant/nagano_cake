<div class="container">
  <h1>ジャンル一覧・追加</h1>
  <div class="border-top border-dark" style="padding:5px;"></div><br>
  <div>
   <%= form_with model:@new_genre, url:admin_genres_path, local:true do |f| %>
   <label class="form-control-label">Genre Name</label>
   <%= f.text_field :genre_name %>
   <%= f.submit 'New', class:"btn btn-secondary btn-sm" %>
   <% end %>
  </div>
  
  <table class="table table-striped">
   <thead class="thead-dark">
    <tr>
     <th>Genre</th>
     <th></th>
    </tr>
   </thead>
   <thody>
    <% @genres.each do |genre| %>
     <tr>
      <td><%= genre.genre_name %></td>
      <td> <%= link_to "Edit", edit_admin_genre_path(genre), class:"btn btn-secondary btn-sm" %></td>
     </tr>
    <% end %>
   </thody>
  </table>
</div>

